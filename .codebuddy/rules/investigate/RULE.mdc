---
description: "系统化根因调试（Iron Law）。当用户说「调试这个」「修复这个 bug」「为什么坏了」「调查这个错误」「根因分析」「debug」「investigate」或报告错误、意外行为时使用。"
alwaysApply: false
enabled: true
---

# /investigate — 系统化根因调试

## Iron Law（铁律）

**先做根因调查，再修复。没有根因调查就没有修复。**

修复症状会制造打地鼠式调试。每一次不针对根因的修复都让下一个 bug 更难找。找到根因，然后修复它。

---

## Phase 1: 根因调查

在形成任何假设之前收集上下文。

1. **收集症状：** 阅读错误消息、堆栈跟踪和复现步骤。如果用户没有提供足够上下文，**一次只问一个问题**。

2. **阅读代码：** 从症状追溯代码路径到潜在原因。使用搜索工具查找所有引用，读取代码理解逻辑。

3. **检查最近变更：**
   ```bash
   git log --oneline -20 -- <affected-files>
   ```
   这之前是工作的吗？什么改变了？回归意味着根因在 diff 中。

4. **复现：** 能否确定性地触发 bug？如果不能，在继续之前收集更多证据。

输出：**"根因假设: ..."** — 一个关于什么是错误的、为什么错误的具体、可测试的声明。

---

## 范围锁定

形成根因假设后，将编辑锁定在受影响的模块中以防止范围蔓延。

确定包含受影响文件的最窄目录。告知用户：
> "本次调试会话专注于 `<dir>/`。这是为了防止对无关代码的修改。"

如果 bug 跨越整个仓库或范围确实不清楚，跳过锁定并说明原因。

---

## Phase 2: 模式分析

检查此 bug 是否匹配已知模式：

| 模式 | 特征 | 在哪里查找 |
|------|------|-----------|
| 竞态条件 | 间歇性的、时序相关的 | 对共享状态的并发访问 |
| Nil/null 传播 | NoMethodError, TypeError | 对可选值缺少守卫 |
| 状态损坏 | 数据不一致、部分更新 | 事务、回调、钩子 |
| 集成失败 | 超时、意外响应 | 外部 API 调用、服务边界 |
| 配置漂移 | 本地正常、staging/prod 失败 | 环境变量、功能开关、数据库状态 |
| 过期缓存 | 显示旧数据、清除缓存后正常 | Redis、CDN、浏览器缓存 |

同时检查：
- `TODOS.md` 中相关的已知问题
- `git log` 中同一区域的先前修复 — **相同文件中反复出现的 bug 是架构味道**，不是巧合

**外部模式搜索：** 如果 bug 不匹配上述已知模式，搜索：
- "{框架} {通用错误类型}" — **先脱敏：** 去掉主机名、IP、文件路径、SQL、客户数据。搜索错误类别，而不是原始消息。
- "{库} {组件} known issues"

---

## Phase 3: 假设检验

在写任何修复之前，验证你的假设。

1. **确认假设：** 在怀疑的根因处添加临时日志语句、断言或调试输出。运行复现。证据匹配吗？

2. **如果假设错误：** 在形成下一个假设之前，搜索错误（**先脱敏**——去掉主机名、IP、文件路径、SQL 片段、客户标识符和任何内部/专有数据）。然后回到 Phase 1。收集更多证据。不要猜测。

3. **3 次打击规则：** 如果 3 个假设都失败了，**停下来**。询问用户：

   ```
   已测试 3 个假设，均不匹配。这可能是架构问题而非简单 bug。

   A) 继续调查 — 我有新假设: [描述]
   B) 升级为人工审查 — 这需要了解系统的人
   C) 添加日志并等待 — 检测该区域，下次捕获
   ```

**红旗** — 如果看到以下任何情况，放慢速度：
- "先临时修复" — 没有"临时"。修对或升级。
- 在追溯数据流之前就提出修复 — 你在猜。
- 每次修复在其他地方揭示新问题 — 错误的层，不是错误的代码。

---

## Phase 4: 实现

一旦根因确认：

1. **修复根因，不是症状。** 消除实际问题的最小变更。

2. **最小差异：** 最少的文件修改，最少的行变更。抵制重构相邻代码的冲动。

3. **写回归测试** 满足：
   - 在没有修复时**失败**（证明测试有意义）
   - 在有修复时**通过**（证明修复有效）

4. **运行完整测试套件。** 粘贴输出。不允许回归。

5. **如果修复涉及 >5 个文件：** 标记爆炸半径：
   ```
   此修复涉及 N 个文件。对于 bug 修复来说这是很大的爆炸半径。
   A) 继续 — 根因确实跨越这些文件
   B) 拆分 — 现在修复关键路径，延迟其余
   C) 重新思考 — 也许有更有针对性的方法
   ```

---

## Phase 5: 验证与报告

**全新验证：** 复现原始 bug 场景并确认已修复。这不是可选的。

运行测试套件并粘贴输出。

输出结构化调试报告：
```
调试报告
════════════════════════════════════════
症状:           [用户观察到的现象]
根因:           [实际上错在哪里]
修复:           [修改了什么，带 file:line 引用]
证据:           [测试输出、复现尝试显示修复有效]
回归测试:       [新测试的 file:line]
相关:           [TODOS.md 项、同区域先前 bug、架构注释]
状态:           DONE | DONE_WITH_CONCERNS | BLOCKED
════════════════════════════════════════
```

---

## 重要规则

- **3+ 次修复尝试失败 → 停下来质疑架构。** 错误的架构，不是失败的假设。
- **永远不要应用你无法验证的修复。** 如果你不能复现并确认，不要发布。
- **永远不要说"这应该能修复它"。** 验证并证明它。运行测试。
- **如果修复涉及 >5 个文件 → 询问用户**关于爆炸半径后再继续。
- **完成状态：**
  - DONE — 根因找到，修复应用，回归测试编写，所有测试通过
  - DONE_WITH_CONCERNS — 修复了但无法完全验证（如间歇性 bug、需要 staging）
  - BLOCKED — 调查后根因不明，已升级
