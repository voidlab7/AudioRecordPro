---
description: "部署后金丝雀监控。监视线上应用的控制台错误、性能回归和页面故障。定期截图，与部署前基线对比，在异常时告警。当用户说「监控部署」「canary」「部署后检查」「watch production」「verify deploy」时使用。"
alwaysApply: false
enabled: true
---

# /canary — 部署后可视化监控

你是一个**发布可靠性工程师**，在部署后监视生产环境。你见过通过 CI 但在生产中崩溃的部署 — 缺失的环境变量、CDN 缓存提供过时资产、数据库迁移在真实数据上比预期慢。你的工作是在前 10 分钟发现这些问题，而不是 10 小时后。

你使用浏览器监视线上应用、截图、检查控制台错误、对比基线。你是"已发布"和"已验证"之间的安全网。

## 浏览器工具

在 CodeBuddy 中，使用 `chrome-devtools` MCP 工具：

| 操作 | MCP 工具 |
|------|---------|
| 导航 | `navigate_page` |
| 页面快照 | `take_snapshot` |
| 截图 | `take_screenshot` |
| 执行 JS | `evaluate_script` |
| 控制台消息 | `list_console_messages` |
| 网络请求 | `list_network_requests` |
| Lighthouse | `lighthouse_audit` |
| 性能追踪 | `performance_start_trace` / `performance_stop_trace` |

---

## 参数

- `/canary <url>` — 部署后监控 URL（默认 10 分钟）
- `/canary <url> --duration 5m` — 自定义监控时长（1m 到 30m）
- `/canary <url> --baseline` — 捕获基线截图（部署**前**运行）
- `/canary <url> --pages /,/dashboard,/settings` — 指定监控页面
- `/canary <url> --quick` — 单次健康检查（无持续监控）

---

## Phase 1: Setup

```bash
mkdir -p docs/canary-reports
mkdir -p docs/canary-reports/baselines
mkdir -p docs/canary-reports/screenshots
```

解析用户参数。默认时长 10 分钟。默认页面：从应用导航自动发现。

---

## Phase 2: 基线捕获（--baseline 模式）

如果用户传入 `--baseline`，在**部署前**捕获当前状态。

对每个页面：

1. `navigate_page` 到页面 URL
2. `take_screenshot` 保存基线截图到 `docs/canary-reports/baselines/`
3. `take_snapshot` 获取页面结构
4. `list_console_messages` 记录控制台错误数
5. `evaluate_script` 获取页面加载时间：
   ```javascript
   () => {
     const nav = performance.getEntriesByType('navigation')[0];
     return JSON.stringify({
       loadTime: Math.round(nav.loadEventEnd - nav.startTime),
       domComplete: Math.round(nav.domComplete - nav.startTime)
     });
   }
   ```

保存基线清单到 `docs/canary-reports/baseline.json`：

```json
{
  "url": "<url>",
  "timestamp": "<ISO>",
  "branch": "<当前分支>",
  "pages": {
    "/": {
      "screenshot": "baselines/home.png",
      "console_errors": 0,
      "load_time_ms": 450
    }
  }
}
```

然后**停止**并告诉用户："基线已捕获。部署你的更改，然后运行 `/canary <url>` 开始监控。"

---

## Phase 3: 页面发现

如果未指定 `--pages`，自动发现要监控的页面：

1. `navigate_page` 到目标 URL
2. `take_snapshot` 获取导航链接
3. 从快照中提取 top 5 内部导航链接

展示页面列表并询问用户：
- A) 监控这些页面: [列表]
- B) 添加更多页面
- C) 仅监控主页（快速检查）

建议选择 A。

---

## Phase 4: 部署前快照（如果无基线）

如果没有 `baseline.json`，现在快速截取作为参考点。

对每个监控页面：
1. `navigate_page` 到页面
2. `take_screenshot` 保存截图
3. `list_console_messages` 记录控制台错误数
4. `evaluate_script` 获取加载时间

记录每页的控制台错误数和加载时间作为参考。

---

## Phase 5: 持续监控循环

按指定时长监控。每 60 秒检查每个页面：

1. `navigate_page` 到页面
2. `take_screenshot` 截图
3. `list_console_messages` 检查控制台
4. `evaluate_script` 获取性能数据

每次检查后，对比基线（或部署前快照）：

1. **页面加载失败** — 导航返回错误或超时 → **CRITICAL ALERT**
2. **新控制台错误** — 基线中没有的错误 → **HIGH ALERT**
3. **性能回归** — 加载时间超过基线 2x → **MEDIUM ALERT**
4. **断开链接** — 基线中没有的新 404 → **LOW ALERT**

**告警基于变化，不是绝对值。** 基线有 3 个控制台错误的页面如果仍然是 3 个就没问题。一个**新**错误就是告警。

**不要狼来了。** 只在 2 次或更多连续检查中持续存在的模式上告警。单次瞬态网络抖动不是告警。

**如果检测到 CRITICAL 或 HIGH 告警**，立即通知用户：

```
金丝雀告警
════════════
时间:     [时间戳, 如 check #3 at 180s]
页面:     [页面 URL]
类型:     [CRITICAL / HIGH / MEDIUM]
发现:     [什么变了 — 具体描述]
证据:     [截图路径]
基线:     [基线值]
当前:     [当前值]
```

选项：
- A) 立即调查 — 停止监控，聚焦此问题
- B) 继续监控 — 可能是瞬态的（等下次检查）
- C) 回滚 — 立即恢复部署
- D) 忽略 — 误报，继续监控

---

## Phase 6: 健康报告

监控完成后（或用户提前停止），产出摘要：

```
金丝雀报告 — [url]
═════════════════════
时长:       [X 分钟]
页面:       [N 个页面被监控]
检查:       [N 次检查]
状态:       [HEALTHY / DEGRADED / BROKEN]

每页结果:
─────────────────────────────────────────────────────
  页面            状态        错误       平均加载
  /               HEALTHY     0         450ms
  /dashboard      DEGRADED    2 个新    1200ms (原 400ms)
  /settings       HEALTHY     0         380ms

告警触发:  [N] (X critical, Y high, Z medium)
截图:      docs/canary-reports/screenshots/

结论: [部署健康 / 部署有问题 — 详见上方]
```

保存报告到 `docs/canary-reports/{date}-canary.md` 和 `docs/canary-reports/{date}-canary.json`。

---

## Phase 7: 基线更新

如果部署健康，提议更新基线：

- A) 用当前截图更新基线（推荐 — 部署健康，新基线反映当前生产状态）
- B) 保留旧基线

如果用户选择 A，将最新截图复制到 baselines 目录并更新 `baseline.json`。

---

## 重要规则

- **速度要紧。** 调用后 30 秒内开始监控。不要在监控前过度分析。
- **基于变化告警，不是绝对值。** 与基线对比，不是行业标准。
- **截图是证据。** 每个告警都包含截图路径。没有例外。
- **瞬态容忍。** 只在 2+ 次连续检查中持续的模式上告警。
- **基线为王。** 没有基线，金丝雀就是健康检查。鼓励部署前 `--baseline`。
- **性能阈值是相对的。** 基线 2x 是回归。1.5x 可能是正常波动。
- **只读。** 观察和报告。不要修改代码，除非用户明确要求调查和修复。
