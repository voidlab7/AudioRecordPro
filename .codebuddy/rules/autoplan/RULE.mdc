---
description: "自动审查流水线——一条命令进，完整审查出。当用户说「自动审查」「autoplan」「运行所有审查」「自动审查这个计划」「帮我做决定」或有一个计划文件想跑完全部审查而不想回答 15-30 个中间问题时使用。"
alwaysApply: false
enabled: true
---

# /autoplan — 自动审查流水线

一条命令。粗略计划进，完整审查出。

/autoplan 按顺序执行 CEO → Design → Eng 三个审查阶段，以完整深度运行每个阶段的全部内容。唯一的区别：中间的问题使用以下 6 个决策原则自动回答，而不是问用户。真正需要人类判断的"品味决策"在最终审批门集中呈现。

---

## 6 个决策原则

这些规则自动回答每个中间问题：

| # | 原则 | 含义 |
|---|------|------|
| 1 | **选择完整性** | 覆盖更多边界情况的方案优先。做完整的事 |
| 2 | **烧干湖水** | 修复爆炸半径内（本计划修改的文件 + 直接依赖者）的所有问题。自动批准在爆炸半径内且 < 1 天工作量的扩展 |
| 3 | **务实** | 两个选项修同一个东西，选更干净的。5 秒选择，不是 5 分钟 |
| 4 | **DRY** | 和现有功能重复？拒绝。复用已有的 |
| 5 | **显式优于聪明** | 10 行明显的修复 > 200 行抽象。新贡献者 30 秒能读懂的 |
| 6 | **倾向行动** | 合并 > 反复审查 > 停滞讨论。标记关注但不阻塞 |

**冲突解决（上下文相关的平局打破）：**
- **CEO 阶段：** P1（完整性）+ P2（烧干湖水）主导
- **Eng 阶段：** P5（显式）+ P3（务实）主导
- **Design 阶段：** P5（显式）+ P1（完整性）主导

---

## 决策分类

每个自动决策被分类为：

**机械性决策** — 只有一个明显正确的答案。静默自动处理。
示例：运行评估（总是是），在一个完整计划上缩减范围（总是否）。

**品味决策** — 合理的人会有分歧。自动决策并附推荐，但在最终审批门呈现。三个自然来源：
1. **接近的方案** — 前两名都可行，有不同的权衡
2. **边界范围** — 在爆炸半径内但 3-5 个文件，或半径模糊
3. **模型分歧** — 如果使用了多次分析且结论不同

---

## 顺序执行 — 强制

阶段必须按严格顺序执行：CEO → Design → Eng。
每个阶段必须在下一个开始前完全完成。
**永远不要并行运行阶段** — 每个阶段建立在前一个之上。

---

## "自动决策"意味着什么

自动决策用 6 个原则替代用户的判断。它**不**替代分析。每个 Section 仍必须以与交互版本相同的深度执行。唯一改变的是谁回答问题：你用 6 个原则来回答。

**你仍然必须：**
- 读取实际代码、diff 和每个 Section 引用的文件
- 产出每个 Section 要求的所有输出（图表、表格、注册表）
- 识别每个 Section 设计用来捕获的每个问题
- 使用 6 个原则决策每个问题
- 将每个决策记录在审计追踪中

**你绝不能：**
- 把一个审查 Section 压缩成一行表格行
- 不展示你检查了什么就写"没发现问题"
- 跳过一个 Section 因为"不适用"而不说明你检查了什么和为什么

---

## Phase 0: 接收 + 恢复点

1. **保存恢复点：** 将计划文件的当前状态保存到外部文件
2. **读取上下文：** 读取 CODEBUDDY.md、TODOS.md、git log、git diff
3. **检查现有设计文档**
4. **检测 UI 范围：** grep 计划中的视图/渲染术语（component、screen、form、button、modal 等）。需要 2+ 匹配

---

## Phase 1: CEO 审查（战略与范围）

遵循 `@plan-ceo-review` 规则的所有部分，以完整深度执行。

**覆盖规则：**
- 模式选择：默认 SELECTIVE EXPANSION
- 前提：接受合理的（P6），只挑战明显错误的
- **关口：呈现前提给用户确认** — 这是唯一不自动决策的问题。前提需要人类判断。
- 替代方案：选择最高完整性（P1）。平局选最简洁（P5）。前两名接近 → TASTE DECISION
- 范围扩展：在爆炸半径内 + <1 天 → 批准（P2）。之外 → 延期（P3）。重复 → 拒绝（P4）
- 所有 10 个审查 Section：完整运行，自动决策每个问题，记录每个决策

**Phase 1 必需输出：**
- "NOT in scope" 部分
- "What already exists" 部分
- 错误和救援注册表
- 失败模式注册表
- 梦想状态差距
- 完成摘要

**PHASE 1 完成。** 发出阶段转换摘要后继续。

---

## Phase 2: 设计审查（条件性 — 如果没有 UI 范围则跳过）

如果检测到 UI 范围，遵循 `@plan-design-review` 规则的所有 7 个维度，以完整深度执行。

**覆盖规则：**
- 关注领域：所有相关维度（P1）
- 结构性问题（缺失状态、破碎的层级）：自动修复（P5）
- 审美/品味问题：标记为 TASTE DECISION
- 设计系统对齐：如果 DESIGN.md 存在且修复明显则自动修复

**Phase 2 必需输出：**
- 所有 7 个维度的评分
- 已识别并自动决策的问题

---

## Phase 3: 工程审查

遵循 `@plan-eng-review` 规则的所有部分，以完整深度执行。

**覆盖规则：**
- 范围挑战：永远不缩减（P2）
- 架构选择：显式优于聪明（P5）
- 评估：总是包含所有相关的测试套件（P1）
- TODOS.md：收集 Phase 1 的所有延期范围扩展，自动写入

**Phase 3 必需输出：**
- "NOT in scope" 部分
- "What already exists" 部分
- 架构 ASCII 图表
- 测试图表映射代码路径到覆盖率
- 失败模式注册表
- 完成摘要
- TODOS.md 更新

---

## 决策审计追踪

每个自动决策后，追加一行到计划文件：

```markdown
## 决策审计追踪

| # | 阶段 | 决策 | 原则 | 理由 | 被拒绝的选项 |
|---|------|------|------|------|-------------|
```

逐步写入每个决策。这保持审计记录在磁盘上，而不是积累在对话上下文中。

---

## Phase 4: 最终审批门

**在这里停下来，将最终状态呈现给用户。**

```
## /autoplan 审查完成

### 计划摘要
[1-3 句话摘要]

### 决策统计: [N] 个总计（[M] 个自动决策，[K] 个需要你选择）

### 你的选择（品味决策）
[对于每个品味决策:]
**选择 [N]: [标题]**（来自 [阶段]）
我推荐 [X] — [原则]。但 [Y] 也可行：
  [如果选 Y 的下游影响，一句话]

### 自动决策: [M] 个决策 [见计划文件中的决策审计追踪]

### 审查评分
- CEO: [摘要]
- Design: [摘要 或 "跳过，无 UI 范围"]
- Eng: [摘要]

### 跨阶段主题
[如果有 2+ 个阶段独立标记了同一个关注点:]
**主题: [话题]** — 在 [Phase 1, Phase 3] 中被标记。高置信度信号。

### 延期到 TODOS.md
[自动延期的项目及理由]
```

**选项：**
- A) 按原样批准（接受所有推荐）
- B) 带覆盖批准（指定哪些品味决策要更改）
- C) 询问（询问任何特定决策）
- D) 修改（计划本身需要更改）
- E) 拒绝（重新开始）

**选项处理：**
- A: 标记 APPROVED，建议下一步
- B: 询问哪些覆盖，应用，重新呈现
- C: 自由回答，重新呈现
- D: 做更改，重新运行受影响的阶段。最多 3 轮
- E: 重新开始

---

## 重要规则

- **永远不要中止。** 用户选择了 /autoplan。尊重这个选择。呈现所有品味决策，永远不要重定向到交互式审查。
- **前提是唯一的关口。** 唯一不自动决策的问题是 Phase 1 中的前提确认。
- **记录每个决策。** 没有静默的自动决策。每个选择在审计追踪中占一行。
- **完整深度就是完整深度。** 不要压缩或跳过审查 Section。如果你发现自己对任何审查 Section 写的不到 3 句话，你可能在压缩。
- **制品是交付物。** 测试计划、失败模式注册表、错误/救援表、ASCII 图表——审查完成时必须存在。
- **顺序执行。** CEO → Design → Eng。每个阶段建立在前一个之上。
