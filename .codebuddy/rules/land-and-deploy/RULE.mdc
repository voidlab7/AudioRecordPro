---
description: "合并部署验证工作流。合并 PR、等待 CI 和部署、验证生产环境健康。接续 /ship 创建 PR 后的工作。当用户说「merge」「land」「deploy」「合并并验证」「部署到生产」时使用。"
ruleType: requested
---

# /land-and-deploy — 合并、部署、验证

你是一位**发布工程师**。这个技能接续 `/ship`。`/ship` 创建 PR，你合并它、等待部署、验证生产。

## 参数
- `/land-and-deploy` — 自动检测 PR
- `/land-and-deploy <url>` — 自动检测 PR，验证此 URL
- `/land-and-deploy #123` — 指定 PR 号
- `/land-and-deploy #123 <url>` — 指定 PR + 验证 URL

## 停止规则

**始终停止：** 预合并就绪门（Step 3.5）、GitHub CLI 未认证、无 PR、CI 失败/冲突、权限被拒、部署失败（提供回滚）、金丝雀检测到问题（提供回滚）

**永不停止：** 选择合并方法、超时警告

---

## Step 0: 检测基础分支

1. `gh pr view --json baseRefName -q .baseRefName`
2. 回退：`gh repo view --json defaultBranchRef -q .defaultBranchRef.name`
3. 都失败则用 `main`。

## Step 1: 预检

1. `gh auth status` — 未认证则停止
2. 解析参数（PR 号、URL）
3. 无 PR 号时从当前分支检测：`gh pr view --json number,state,title,url,mergeStateStatus,mergeable,baseRefName,headRefName`
4. 验证状态：无 PR → 停止；MERGED → 无需操作；CLOSED → 先重新打开；OPEN → 继续

## Step 2: 预合并检查

```bash
gh pr checks --json name,state,status,conclusion
gh pr view --json mergeable -q .mergeable
```
失败 → 停止；待处理 → Step 3；通过 → Step 3.5；CONFLICTING → 停止

## Step 3: 等待 CI（如果待处理）

```bash
gh pr checks --watch --fail-fast
```
15 分钟超时。通过 → Step 3.5；失败 → 停止。

## Step 3.5: 预合并就绪门

**关键安全检查。** 收集证据，构建就绪报告，获取用户确认。

### 检查项
1. **测试结果：** 运行项目测试命令。失败 = BLOCKER。
2. **PR 描述准确性：** 比较 PR body 和实际提交，检查遗漏/过期/版本号错误。
3. **文档发布：** 检查 CHANGELOG.md 和 VERSION 是否更新。
4. **就绪报告：** ASCII 表格展示所有检查状态。

询问用户：A) 合并  B) 暂不合并  C) 仍然合并（了解风险）

## Step 4: 合并 PR

```bash
gh pr merge --auto --delete-branch
# 如果 --auto 不可用：
gh pr merge --squash --delete-branch
```
权限错误 → 停止。合并队列 → 每 30 秒轮询，最多 30 分钟。

## Step 5: 部署策略检测

自动检测平台（fly.toml、vercel.json、netlify.toml、Procfile 等）和部署工作流。

**决策树：**
1. 用户提供 URL → 金丝雀验证
2. 检测到部署工作流 → 轮询 + 金丝雀
3. 仅文档更改 → 跳过验证
4. 无工作流无 URL → 询问用户

## Step 6: 等待部署

- **GitHub Actions：** `gh run list` + `gh run view` 轮询
- **平台 CLI：** fly/render/heroku 对应命令
- **自动部署（Vercel/Netlify）：** 等 60 秒后金丝雀

部署失败 → 询问：A) 调查日志  B) 回滚  C) 继续

## Step 7: 金丝雀验证

使用 `chrome-devtools` MCP 工具：

1. **导航**（`navigate_page`）→ 检查 200
2. **控制台错误**（`list_console_messages`）→ 过滤 Error/Uncaught/TypeError
3. **性能**（`evaluate_script` + Performance API）→ < 10s
4. **内容**（`take_snapshot`）→ 非空白/非错误页
5. **截图**（`take_screenshot`）→ `docs/deploy-reports/post-deploy.png`

全部通过 → HEALTHY。失败 → 询问：A) 预期的  B) 回滚  C) 调查

## Step 8: 回滚（如需要）

```bash
git fetch origin <base> && git checkout <base>
git revert <merge-commit-sha> --no-edit
git push origin <base>
```

## Step 9: 部署报告

```
LAND & DEPLOY REPORT
═════════════════════
PR:           #N — title
Branch:       feature → main
Merged:       timestamp (method)
Timing:       CI Xm / Queue Xm / Deploy Xm / Canary Xm / Total Xm
VERDICT:      DEPLOYED AND VERIFIED / DEPLOYED (UNVERIFIED) / REVERTED
```

保存到 `docs/deploy-reports/{date}-pr{number}-deploy.md`。

## Step 10: 建议后续

- `/canary <url>` 扩展监控
- `/benchmark <url>` 性能审计
- `/document-release` 文档更新

## 重要规则

- **永不 force push。** 永不跳过 CI。自动检测一切。
- **回滚始终是选项。** 一次性验证（`/canary` 做持续监控）。
- **目标：用户说 `/land-and-deploy`，下一个看到的就是部署报告。**
