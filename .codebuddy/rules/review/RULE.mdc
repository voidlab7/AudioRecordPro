---
description: "PR 预合并审查。分析当前分支 diff，检测 SQL 安全、LLM 信任边界违规、竞态条件、枚举完整性等结构性问题。当用户说「审查这个 PR」「代码审查」「预合并审查」「review」「check my diff」或即将合并代码时使用。"
alwaysApply: false
enabled: true
---

# /review — PR 预合并审查

你正在执行 `/review` 工作流。分析当前分支对基础分支的 diff，寻找测试无法捕获的结构性问题。

---

## Step 1: 检查分支

1. 运行 `git branch --show-current` 获取当前分支。
2. 如果在基础分支上，输出：**"没什么可审查的 — 你在基础分支上或没有针对它的更改。"** 然后停止。
3. 检测基础分支：
   - 先尝试：`gh pr view --json baseRefName -q .baseRefName 2>/dev/null`
   - 失败则：`gh repo view --json defaultBranchRef -q .defaultBranchRef.name 2>/dev/null`
   - 都失败则用 `main`
4. 运行 `git fetch origin <base> --quiet && git diff origin/<base> --stat` 检查是否有 diff。无 diff 则输出相同消息并停止。

---

## Step 1.5: 范围偏移检测

在审查代码质量之前，先检查：**他们是否构建了被要求的东西 — 不多不少？**

1. 读取 `TODOS.md`（如果存在）。读取 PR 描述 (`gh pr view --json body --jq .body 2>/dev/null || true`)。
   读取提交信息 (`git log origin/<base>..HEAD --oneline`)。
   **如果没有 PR：** 依靠提交信息和 TODOS.md 判断声明意图。
2. 识别**声明意图** — 这个分支应该完成什么？
3. 运行 `git diff origin/<base>...HEAD --stat`，将修改的文件与声明意图对比。
4. 怀疑式评估：

   **范围蔓延检测：**
   - 与声明意图无关的文件被修改
   - 计划中未提及的新功能或重构
   - "顺手改一下"扩大了影响半径

   **缺失需求检测：**
   - TODOS.md/PR 描述中的需求未在 diff 中体现
   - 声明需求缺少测试覆盖
   - 部分实现（开始但未完成）

5. 输出（在主审查开始前）：
   ```
   范围检查: [CLEAN / DRIFT DETECTED / REQUIREMENTS MISSING]
   意图: <一行总结被请求的内容>
   交付: <一行总结 diff 实际做了什么>
   [如有偏移: 列出每个超范围变更]
   [如有缺失: 列出每个未完成需求]
   ```

6. 这是**信息性**的 — 不阻断审查。继续到 Step 2。

---

## Step 2: 读取审查清单

审查清单内嵌如下，按两遍审查执行。

---

## Step 3: 获取 diff

获取最新基础分支以避免过时本地状态的误报：

```bash
git fetch origin <base> --quiet
```

运行 `git diff origin/<base>` 获取完整 diff。这包括针对最新基础分支的已提交和未提交更改。

---

## Step 4: 两遍审查

### Pass 1 — CRITICAL（最高严重性）

#### SQL 和数据安全
- SQL 中的字符串插值（即使值是 `.to_i`/`.to_f` — 使用参数化查询）
- TOCTOU 竞争：检查-然后-设置模式应该是原子 `WHERE` + `update_all`
- 绕过模型验证的直接 DB 写入
- N+1 查询：循环/视图中缺少预加载

#### 竞态条件和并发
- 读取-检查-写入没有唯一性约束或捕获重复键错误并重试
- find-or-create 没有唯一 DB 索引 — 并发调用可能创建重复
- 状态转换没有使用原子 `WHERE old_status = ? UPDATE SET new_status`
- 用户控制数据的不安全 HTML 渲染（dangerouslySetInnerHTML / v-html / .html_safe 等）— XSS

#### LLM 输出信任边界
- LLM 生成的值（邮箱、URL、名称）写入 DB 或传递给邮件服务前未做格式验证
- 结构化工具输出（数组、哈希）在写入数据库前未做类型/形状检查

#### 枚举和值完整性
当 diff 引入新的枚举值、状态字符串、层级名称或类型常量时：
- **追踪每个消费者。** 搜索（不只是 grep — 要 READ）每个切换、过滤或显示该值的文件
- **检查允许列表/过滤数组。** 搜索包含同类值的数组
- **检查 `case`/`if-elsif` 链。** 新值是否会掉入错误的默认分支？

### Pass 2 — INFORMATIONAL（较低严重性）

#### 条件副作用
- 分支于条件但忘记在某分支应用副作用的代码路径
- 日志消息声称动作已发生但动作被条件跳过

#### 魔法数字和字符串耦合
- 多文件中使用的裸数字字面量 — 应为命名常量
- 错误消息字符串在其他地方用作查询过滤器

#### 死代码和一致性
- 赋值但从未读取的变量
- PR 标题与 VERSION/CHANGELOG 文件之间的版本不匹配
- 代码更改后描述旧行为的注释/文档字符串

#### LLM Prompt 问题
- Prompt 中的 0-索引列表（LLM 可靠地返回 1-索引）
- Prompt 文本列出的可用工具/能力与实际配置的不匹配

#### 测试缺口
- 负路径测试只断言类型/状态但不验证副作用
- 缺少安全执行特性（阻断、限速、认证）的端到端集成测试

#### 完整性缺口
- 当完整版本 <30 分钟即可完成时的快捷实现
- 80-90% 的功能实现而 100% 用少量代码即可达到

#### 加密和熵
- 截断数据而非哈希（SHA-256 优于取最后 N 个字符）
- 安全敏感值使用 `rand()` 而非 `SecureRandom`
- 对密钥或令牌使用非恒定时间比较（`==`）— 易受时间攻击

#### 时间窗口安全
- 假设"今天"覆盖 24 小时的日期键查询
- 相关功能之间不匹配的时间窗口

#### 类型强制转换边界
- 值跨越语言→JSON→JS 边界时类型可能变化
- 哈希/摘要输入序列化前未调用 `.to_s` 或等效方法

#### 视图/前端
- 部分模板中的内联 `<style>` 块（每次渲染重新解析）
- 视图中的 O(n*m) 查找
- Ruby 端 `.select{}` 过滤应该是 `WHERE` 子句的 DB 结果

#### 性能和打包影响
- 已知重量级新 `dependencies`：moment.js（→ date-fns）、完整 lodash（→ lodash-es）、jquery
- 无 `loading="lazy"` 或显式宽高属性的图片（导致 CLS）
- 提交到仓库的大型静态资产（>500KB/文件）
- 无 async/defer 的同步 `<script>` 标签

**不要标记：**
- devDependencies 添加（不影响生产打包）
- 动态 `import()` 调用（代码分割 — 这是好的）
- 小型工具添加（<5KB gzipped）
- 仅服务端依赖

---

## Step 4.5: 测试覆盖图

在审查 diff 后，绘制测试覆盖图：

1. 列出 diff 中每个被修改的函数/方法/组件
2. 对每个，搜索是否有对应的测试文件和测试用例
3. 绘制覆盖图：

```
测试覆盖图
════════════
✅ app/models/user.rb → test/models/user_test.rb (12 assertions)
⚠️  app/services/billing.rb → test/services/billing_test.rb (3 assertions, 缺少负路径)
❌ app/controllers/api/v2/orders_controller.rb → 无测试
```

4. 标记缺口为 INFORMATIONAL 发现

---

## Step 5: 先修后问（Fix-First Review）

**每个发现都获得行动 — 不只是关键发现。**

输出摘要标题：`预合并审查: N 个问题 (X 关键, Y 信息性)`

### Step 5a: 分类每个发现

对每个发现，按 Fix-First 启发式分类为 AUTO-FIX 或 ASK：

```
AUTO-FIX（直接修复不询问）：          ASK（需要人工判断）：
├─ 死代码 / 未使用变量               ├─ 安全（认证、XSS、注入）
├─ N+1 查询（缺少预加载）            ├─ 竞态条件
├─ 与代码矛盾的过时注释              ├─ 设计决策
├─ 魔法数字 → 命名常量               ├─ 大型修复（>20 行）
├─ 缺少 LLM 输出验证                 ├─ 枚举完整性
├─ 版本/路径不匹配                   ├─ 删除功能
└─ 内联样式、O(n*m) 视图查找         └─ 任何改变用户可见行为的

经验法则：如果修复是机械性的且资深工程师会不经讨论就应用，那就是 AUTO-FIX。
如果合理的工程师可能对修复方式有不同意见，那就是 ASK。
关键发现默认偏向 ASK。信息性发现默认偏向 AUTO-FIX。
```

### Step 5b: 自动修复所有 AUTO-FIX 项

直接应用每个修复。对每个，输出一行摘要：
`[AUTO-FIXED] [file:line] 问题 → 做了什么`

### Step 5c: 批量询问 ASK 项

如果有 ASK 项剩余，在一个回复中展示：

- 列出每项的编号、严重性标签、问题和推荐修复
- 对每项提供选项：A) 按推荐修复 B) 跳过
- 包含总体建议

### Step 5d: 应用用户批准的修复

对用户选择"修复"的项目应用修复。输出修复内容。

### 声明验证

在产出最终审查输出前：
- 如果你声称"这个模式是安全的" → 引用证明安全的具体行
- 如果你声称"这在其他地方处理了" → 读取并引用处理代码
- 如果你声称"测试覆盖了这个" → 命名测试文件和方法
- 永远不要说"可能处理了"或"大概测试了" — 验证或标记为未知

**合理化预防：** "这看起来没问题"不是一个发现。要么引用证据证明它没问题，要么标记为未验证。

---

## Step 5.5: TODOS 交叉引用

读取仓库根目录的 `TODOS.md`（如果存在）。交叉引用 PR 与开放 TODOs：

- **此 PR 是否关闭了任何开放 TODO？** 如果是，在输出中注明
- **此 PR 是否创建了应该成为 TODO 的工作？** 如果是，标记为信息性发现
- **是否有相关 TODO 为此审查提供上下文？** 如果是，讨论相关发现时引用

如果 TODOS.md 不存在，静默跳过。

---

## Step 5.6: 文档过时检查

将 diff 与文档文件交叉引用。对仓库根目录中的每个 `.md` 文件：

1. 检查 diff 中的代码更改是否影响该文档描述的功能/组件/工作流
2. 如果文档在此分支中**未更新**但其描述的代码**已更改**，标记为 INFORMATIONAL 发现：
   "文档可能已过时: [文件] 描述了 [功能/组件] 但代码在此分支中已更改。"

---

## Step 6: 对抗性审查

假装你是一个试图找到审查者遗漏问题的攻击者。快速扫描：

1. **快乐路径偏见：** 你只审查了正常情况吗？错误路径呢？
2. **假设检查：** 你接受了哪些无证据的假设？
3. **盲点：** 有没有你因为"看起来没问题"而跳过的代码？

如果对抗性扫描发现新问题，添加到发现列表。

---

## 抑制项 — 不要标记这些

- "X 与 Y 冗余"但冗余无害且有助可读性
- "添加注释解释为什么选择这个阈值/常量" — 阈值在调优中变化，注释会腐烂
- "这个断言可以更严格"但断言已覆盖行为
- 建议纯一致性变更
- "正则没处理边界情况 X"但输入受限且 X 实践中从不出现
- "测试同时测试了多个守卫" — 没问题
- 评估阈值变更 — 经验性调优，经常变化
- 无害的空操作
- diff 中**已经解决**的任何问题 — 先完整读取 diff 再评论

---

## 重要规则

- **先读取完整 diff 再评论。** 不要标记 diff 中已解决的问题。
- **先修后问，不是只读。** AUTO-FIX 项直接应用。ASK 项仅在用户批准后应用。永远不要提交、推送或创建 PR — 那是用户的事。
- **简洁。** 一行问题，一行修复。无前言。
- **只标记真实问题。** 跳过没问题的内容。
