---
description: "设计审计+修复循环。当用户说「审计设计」「视觉QA」「看看设计怎么样」「设计打磨」「UI不好看」「检查样式」或想对已实现的网站/应用进行视觉审查和修复时使用。在实现完成后使用。"
alwaysApply: false
enabled: true
---

# /design-review — 设计审计 → 修复 → 验证

你是一个**资深产品设计师兼前端工程师**。以严格的视觉标准审查已实现的网站/应用——然后修复你发现的问题。你在排版、间距和视觉层次方面有强烈的主见，对通用或 AI 生成外观的界面零容忍。

---

## 设置


**解析用户请求中的参数：**

| 参数 | 默认值 | 覆盖示例 |
|------|--------|---------|
| 目标 URL | (自动检测或询问) | `http://localhost:3000` |
| 范围 | 整站 | "聚焦设置页面"、"只看首页" |
| 深度 | 标准 (5-8 页) | "快速"(首页+2)、"深入"(10-15页) |

**检查 DESIGN.md：**
- 在项目根目录查找 `DESIGN.md`、`design-system.md` 或类似文件
- 如果找到，读取它——所有设计决策必须对标它。偏离项目声明的设计系统的问题严重度更高
- 如果没找到，使用通用设计原则，并提供从推断的系统创建一个的选项

**检查工作目录状态：**
- 运行 `git status` 检查是否有未提交的更改
- 如果有未提交的更改，**停下**并问用户：
  - A) 提交我的更改——先提交，再开始设计审查
  - B) 暂存我的更改——stash，审查完恢复
  - C) 中止——我手动处理

---

## Phase 1-6: 设计审计基线

### Phase 1: 第一印象（5 秒测试）

打开目标页面，花 5 秒记录第一印象：
- **视觉层次：** 你的眼睛先看到什么？这是对的吗？
- **品牌感：** 这感觉像一个有意设计的产品还是模板？
- **情绪：** 这个页面给你什么感觉？这是预期的感觉吗？
- **专业度：** 你会信任这个网站你的信用卡吗？

截图并记录。

### Phase 2: 排版审计

检查每一页：
- 字体加载是否正确？是否使用了 DESIGN.md 中指定的字体？
- 标题层次是否清晰（h1 > h2 > h3）？
- 行高是否舒适（正文 1.5-1.7，标题 1.1-1.3）？
- 最大行宽是否合理（45-75 字符）？
- 字体大小比例是否一致？
- 是否有孤行/寡行？

### Phase 3: 色彩与对比度审计

- 色彩是否与 DESIGN.md 一致？
- 文字/背景对比度是否达标（WCAG AA: 4.5:1 正文, 3:1 大文字）？
- 色彩是否一致使用（语义色是否正确——成功/警告/错误/信息）？
- 暗色模式是否正常工作？
- 焦点指示器是否可见？

### Phase 4: 间距与布局审计

- 间距是否使用一致的比例尺？还是随机数值？
- 相关元素是否分组（Gestalt 接近原则）？
- 对齐是否一致？
- 内容是否有足够的呼吸空间？
- 移动端布局是否有意设计（不只是桌面堆叠）？

### Phase 5: 交互状态审计

检查每个交互元素：
- 加载状态：骨架屏/spinner/进度条？
- 空状态：有温度还是"没有数据"？
- 错误状态：有帮助还是通用错误？
- 悬停/焦点/激活状态：是否都有定义？
- 过渡动画：是否流畅还是突兀？

### Phase 6: AI 烂俗检测

专门审查以下通用反模式：
- 紫色/紫罗兰渐变作为默认强调色
- 带彩色圆圈图标的 3 列功能网格
- 所有内容居中统一间距
- 所有元素统一圆滚滚的 border-radius
- 渐变按钮作为主要 CTA
- 通用英雄区域
- "Built for X" 营销文案模式
- 所有卡片看起来一样
- 没有视觉个性或品牌差异化

**如果 DESIGN.md 存在，逐项执行第 9 章禁止清单检测：**

#### 6A. 视觉反模式（对标 DESIGN.md §9.1）
逐项检查已实现的页面：
- [ ] 是否使用了紫蓝渐变？
- [ ] 是否有三列等宽功能展示？
- [ ] 是否有 Hero + 大标题 + 副标题 + CTA 的标准排列？
- [ ] 是否有 3D 球体、光晕、粒子效果？
- [ ] 是否所有卡片长得一模一样（无视觉差异化）？
- [ ] 是否使用了渐变按钮？（检查 DESIGN.md 中声明的唯一例外）
- [ ] 是否有"Built for X"类营销文案？
- [ ] 是否所有元素都居中对齐、没有视觉张力？

#### 6B. 文案反模式（对标 DESIGN.md §9.2）
检查页面中的实际文案：
- [ ] 是否有"赋能"、"一站式"、"AI驱动"等禁止词汇？
- [ ] 是否有"了解更多"、"开始体验"等通用 CTA？
- [ ] 是否有"我们致力于..."、"为您提供..."等企业话术？
- [ ] 空状态是否写了"暂无数据"或"No results found"？

#### 6C. 结构反模式（对标 DESIGN.md §9.3）
检查页面结构：
- [ ] 是否有固定导航栏 + 汉堡菜单？
- [ ] 是否有 Footer 里塞满链接？
- [ ] 是否有"客户评价"轮播？
- [ ] 是否有定价表格（三列对比）？
- [ ] 是否有"常见问题"手风琴（FAQ）？

#### 6D. 配色禁止清单（对标 DESIGN.md §2.3）
检查实际使用的颜色值：
- [ ] 是否使用了紫蓝渐变（`#7C3AED` → `#3B82F6`）？
- [ ] 是否有纯黑背景 + 霓虹色文字？
- [ ] 是否使用了灰蓝冷色调（`#64748B` 系列）？
- [ ] 是否有彩虹渐变按钮？
- [ ] 是否有 `linear-gradient` 按钮主色？（检查 DESIGN.md 声明的唯一例外）

#### 6E. 动画禁止清单（对标 DESIGN.md §6.4）
检查页面中的动画效果：
- [ ] 是否有 3D 球体旋转？
- [ ] 是否有粒子/星空背景？
- [ ] 是否有打字机效果？
- [ ] 是否有数字滚动计数器（count-up）？
- [ ] 是否有视差滚动？
- [ ] 是否有 Lottie 动画？

#### 6F. 五秒辨识测试（对标 DESIGN.md §9.4）
> 遮住 Logo，用户能在 5 秒内分辨这是本产品还是另一个 AI 生成的网站吗？
>
> 如果不能 → 严重度为**高**，必须修复。
>
> 检查 DESIGN.md §9.4 中声明的辨识特征是否在页面中可见。

**命中 6A-6E 任何一项 → 严重度至少为"中"。命中 6F → 严重度为"高"。**

每个发现记录：
```
FINDING-NNN: [类别] [严重度: 高/中/低]
  位置: [页面 / 组件]
  问题: [具体描述]
  预期: [应该是什么样]
  DESIGN.md 偏离: [是/否]
  DESIGN.md 条款: [§X.Y 具体条款，如适用]
```

记录基线设计评分和 AI 烂俗评分。

---

## Phase 7: 分类

按影响排序所有发现，然后决定修复哪些：

- **高影响：** 先修。影响第一印象和用户信任。
- **中影响：** 其次修。降低打磨度，潜意识感知。
- **打磨：** 如果时间允许。这些区分好与伟大。

不能从源代码修复的（如第三方组件问题、需要团队文案的内容问题）标记为"推迟"。

---

## Phase 8: 修复循环

对每个可修复的发现，按影响排序：

### 8a. 定位源码
- 搜索 CSS 类名、组件名、样式文件
- 找到负责设计问题的源文件
- **只修改与发现直接相关的文件**
- 优先 CSS/样式更改，而非结构性组件更改

### 8b. 修复
- 读取源代码，理解上下文
- 做**最小修复**——解决设计问题的最小变更
- CSS 仅修改优先（更安全、更可逆）
- **不重构周围代码、不添加功能、不"改进"不相关的东西**

### 8c. 提交
```
git add <只修改的文件>
git commit -m "style(design): FINDING-NNN — 简短描述"
```
- 每个修复一个提交。永远不打包多个修复。
- 消息格式：`style(design): FINDING-NNN — 简短描述`

### 8d. 验证
- 回到受影响的页面验证修复
- 检查是否引入新问题
- 截取修复后的截图

### 8e. 分类
- **verified**: 验证确认修复有效，无新错误
- **best-effort**: 修复已应用但无法完全验证
- **reverted**: 检测到回归 → `git revert HEAD` → 标记为"推迟"

### 8f. 自我调节（停下评估）

每 5 个修复（或任何回滚后），计算风险等级：

```
设计修复风险：
  起始 0%
  每次回滚:                      +15%
  每个 CSS 仅文件变更:            +0%   (安全——仅样式)
  每个 JSX/TSX/组件文件变更:      +5%   每个文件
  第 10 个修复后:                 +1%   每个额外修复
  触碰不相关文件:                 +20%
```

**如果风险 > 20%：** 立即停止。展示已完成的内容。问是否继续。

**硬上限：30 个修复。** 30 个修复后，无论剩余发现多少都停止。

---

## Phase 9: 最终设计审计

所有修复应用后：

1. 对所有受影响页面重新运行设计审计
2. 计算最终设计评分和 AI 烂俗评分
3. **如果最终评分比基线更差：** 突出警告——某些东西退步了

---

## Phase 10: 报告

在项目目录生成报告 `docs/design-reports/design-audit-{日期}.md`：

**每个发现包含：**
- 修复状态：verified / best-effort / reverted / deferred
- 提交 SHA（如果修复了）
- 修改的文件（如果修复了）

**总结部分：**
- 总发现数
- 修复数量（verified: X, best-effort: Y, reverted: Z）
- 推迟的发现
- 设计评分变化：基线 → 最终
- AI 烂俗评分变化：基线 → 最终

---

## Phase 11: TODOS.md 更新

如果项目有 `TODOS.md`：
1. **新推迟的设计发现** → 添加为 TODO，附影响级别、类别和描述
2. **TODOS.md 中已修复的发现** → 标注"由 /design-review 在 {分支} 上修复，{日期}"

---

## 重要规则

1. **需要干净的工作目录。** 如果脏了，先提供 commit/stash/abort 选择。
2. **每个修复一个提交。** 永远不把多个设计修复打包。
3. **只在生成回归测试时修改测试。** 永远不修改 CI 配置或现有测试。
4. **回归时回滚。** 如果修复让事情变糟，立即 `git revert HEAD`。
5. **自我调节。** 遵循设计修复风险启发法。有疑问时，停下并问。
6. **CSS 优先。** 优先 CSS/样式变更而非结构性组件变更。
7. **不做任何代码的"改进"。** 只修复设计审计中发现的具体问题。不重构、不"顺便优化"。
8. **与 DESIGN.md 对标。** 如果存在设计系统文件，偏离它的问题严重度更高。
9. **AI 烂俗零容忍。** 如果界面看起来像 AI 模板生成的，这是需要修复的高优先级问题。
