---
description: 小红书发布 Skill。当用户需要发布内容到小红书时使用此 Skill。触发关键词包括：发布小红书、发小红书、xhs publish、发布到小红书、小红书发布、发笔记、发个小红书。
alwaysApply: false
enabled: true
updatedAt: 2026-03-25T15:48:53.158Z
provider: 
---

# 小红书发布 Skill

小红书图文内容发布自动化工作流。当用户需要发布内容到小红书时使用此 Skill。

**小红书运营三件套**：本规则定义**发布流程**（怎么发），角色人设和写作规范见 `xhs-pet-operator.mdc`（谁），每日运营 SOP 见 `xhs-pet-agent.mdc`（做什么）。

## 触发关键词

- 发布小红书
- 发小红书
- xhs publish
- 发布到小红书
- 小红书发布
- 发笔记
- 发个小红书

## 发布工作流

### 步骤 1：检查登录状态

```
调用 MCP: xiaohongshu.check_login_status
```

如果未登录：
```
调用 MCP: xiaohongshu.get_login_qrcode
提示用户扫码登录
等待用户确认后再次检查登录状态
```

### 步骤 2：准备发布内容

**必需参数**：
| 参数 | 说明 | 限制 |
|------|------|------|
| `title` | 标题 | 最多 20 个中文字 |
| `content` | 正文 | 不包含 `#` 话题标签 |
| `images` | 图片路径列表 | 至少 1 张，支持本地路径或 URL |

**可选参数**：
| 参数 | 说明 | 默认值 |
|------|------|--------|
| `tags` | 话题标签数组 | `[]` |
| `is_original` | 是否声明原创 | `false` |
| `schedule_at` | 定时发布时间 | 立即发布 |
| `visibility` | 可见范围 | 公开可见 |
| `products` | 商品关键词 | `[]` |

### 步骤 3：内容预处理

1. **标题处理**：
   - 检查标题长度 ≤ 20 中文字
   - 移除 emoji（如需保留告知用户可能被截断）

2. **正文处理**：
   - 从正文中提取 `#xxx` 话题标签 → 移到 `tags` 数组
   - 移除特殊字符（如 `—` 转为 `-`）
   - 保留换行符 `\n`

3. **图片处理**：
   - 验证本地图片路径存在
   - 检查图片格式（支持 png/jpg/jpeg/webp）
   - 按封面 → 详情 → 引导顺序排列

### 步骤 4：发布内容

```
调用 MCP: xiaohongshu.publish_content
参数:
  title: string
  content: string (不含 # 标签)
  images: string[] (本地绝对路径或 URL)
  tags: string[] (可选)
  is_original: boolean (可选)
  schedule_at: string (可选, ISO8601 格式)
```

### 步骤 5：确认发布结果

成功时返回：
```
✅ 发布成功！
标题: xxx
配图: x 张
话题: #xxx #yyy
```

失败时返回错误信息并建议解决方案。

## 常见问题处理

### 图片上传失败
- 检查图片路径是否正确
- 检查图片大小（建议 < 5MB）
- 尝试减少图片数量（先传 1 张测试）

### 标题过长
- 自动截断或建议用户缩短
- 小红书限制：最多 20 个中文字或英文单词

### 内容包含敏感词
- 提示用户修改内容
- 建议替换敏感词

### 登录失效
- 删除 cookies 重新登录
```
调用 MCP: xiaohongshu.delete_cookies
调用 MCP: xiaohongshu.get_login_qrcode
```

## 示例用法

### 基础发布
```
用户：发布小红书，标题"测试标题"，内容"这是正文"，图片用 /path/to/image.png
```

### 带话题发布
```
用户：发布小红书
- 标题：我的标题
- 内容：正文内容
- 图片：/path/to/1.png, /path/to/2.png
- 话题：#话题1 #话题2
```

### 定时发布
```
用户：发布小红书，定时到明天早上 10 点
```

### 从内容文件发布
```
用户：读取 xiaohongshu-content/小红书首发内容.md 里的内容发布到小红书
```

## MCP 工具参考

### check_login_status
检查登录状态，返回是否已登录和用户名。

### get_login_qrcode
获取登录二维码（Base64 图片），有效期约 5 分钟。

### delete_cookies
删除 cookies 文件，重置登录状态。

### publish_content
发布图文内容到小红书。

**参数 Schema**：
```json
{
  "title": "string (必需, ≤20字)",
  "content": "string (必需, 不含#标签)",
  "images": "string[] (必需, 至少1张)",
  "tags": "string[] (可选)",
  "is_original": "boolean (可选)",
  "schedule_at": "string (可选, ISO8601)",
  "visibility": "string (可选)",
  "products": "string[] (可选)"
}
```

## 注意事项

1. **内容与 tags 分离**：`content` 中不要包含 `#` 开头的话题标签，所有标签用 `tags` 数组传递
2. **图片路径**：优先使用本地绝对路径，URL 会自动下载
3. **发布频率**：避免短时间内频繁发布，建议间隔 10 分钟以上
4. **账号切换**：如需切换账号，先 `delete_cookies` 再重新登录
5. **发布后禁止自动重试（强制规则）**：发布操作只执行一次，无论结果如何（成功、失败、超时），都必须等待用户手动确认后才能决定下一步。超时≠失败，自动重试会导致发布重复内容。超时时应提醒用户去小红书 App 检查发布状态。