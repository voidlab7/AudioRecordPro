# 2026-03-20 工作日志

## PawMBTI 项目分析与改造

### CDN 资源带宽分析
- 项目部署地址：https://microlab.top/（OpenClaw 服务器，Nginx）
- 唯一外部 CDN：Google Fonts（Quicksand, Noto Sans SC, Fredoka）
- 构建产物总大小：534KB（未压缩），gzip 后约 156KB
- 详细带宽分析已提供给用户

### 真实累计测评计数功能
- 原方案：localStorage 本地存储，初始值 12345（假数据）
- 新方案：独立 Node.js 微服务（`server/count-server.cjs`），JSON 文件持久化
- API: GET/POST `/api/count`，Nginx 反向代理到 3001 端口
- 修改了 quiz-store.ts（fetchTestCount + incrementTestCount 改为 async API 调用）
- 修改了 HomePage.tsx（useEffect 加载真实计数）
- 修改了 QuizPage.tsx（适配 async incrementTestCount）
- 提供了 Nginx 配置参考（`server/nginx-example.conf`）

### Google Fonts 字体优化
- Google Fonts → 国内镜像 `fonts.loli.net`（解决国内无法访问问题）
- 精简字重：11 个字重 → 5 个（Quicksand 400/700, Noto Sans SC 400/700, Fredoka 700）
- 在 index.html 添加 `<link rel="preconnect">` DNS 预连接
- tailwind.config.ts 字体回退链加入 PingFang SC / Microsoft YaHei 等国内常用字体
- 预估字体体积从 500KB-1MB+ 降低到 ~200-300KB

### Git 仓库结构修正
- `web_site/` 是独立私有仓库 `zy20250707/web_site`，不应提交到公开的 `AIAgent` 仓库
- 之前误将 web_site 源码提交到 AIAgent，已修复：
  - 从 AIAgent 仓库 `git rm --cached` 移除 web_site 全部文件
  - AIAgent `.gitignore` 添加 `web_site/` 排除规则
  - 重新在 `web_site/` 初始化 `.git`，关联到 `https://github.com/zy20250707/web_site.git`
  - 字体优化等改动已推送到私有仓库
- **仓库关系**：AIAgent（公开）和 web_site（私有）是同级独立仓库，web_site 目录在 AIAgent 工作目录下但被 gitignore 排除

### 敏感文件清理（git filter-repo）
- 使用 `git filter-repo` 彻底清除 AIAgent 历史中的 `产品IDEA/` 和 `web_site/`
- 本地文件从备份恢复（AIAgent_backup_20260320_120351）
- `.gitignore` 添加 `产品IDEA/` 排除规则
- 本地 git 历史已清理（4个干净提交），远程待 force push 覆盖

### web_site 构建
- 重新安装依赖（node_modules 在备份恢复后不完整）
- 成功构建生产版本：`npm run build`
  - dist/index.html: 1.13 kB (gzip: 0.70 kB)
  - dist/assets/index-*.css: 23.81 kB (gzip: 5.04 kB)
  - dist/assets/*.js: 516.26 kB (gzip: 154.44 kB)

### v1.0.1 发布
- 版本号从 1.0.0 → 1.0.1
- 新增功能：页面底部版本号显示（通过 Vite define 注入 `__APP_VERSION__`）
- 新增部署脚本：`scripts/deploy_website.sh`、`scripts/update_website.sh`
- 新增文档：`SERVER_DEPLOY_GUIDE.md`
- 提交 commit: `72294a6`
- Git tag: `v1.0.1` 已推送
- GitHub Release: https://github.com/zy20250707/web_site/releases/tag/v1.0.1 已创建
- 服务器部署：需要在服务器执行 `bash /root/scripts/deploy_website.sh v1.0.1` 更新
