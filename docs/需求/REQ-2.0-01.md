# REQ-2.0-01 轨道+轨道板模式 UI 重构（录编一体化）

## 版本：V2.0 | 优先级：P0 | 状态：✅ 已实现（Phase 1）

## 实现概述

### 新增组件
| 文件 | 功能 |
|------|------|
| `TitleBarView.swift` | 自定义标题栏 — App名称 + 录制目标 + 导出按钮 |
| `EditToolbarView.swift` | 顶部编辑工具栏 — 裁剪/标准化/淡入/淡出，两态自动切换 |
| `TrackPanelView.swift` | 左侧轨道控制板 — 喇叭图标(Mute)/S(Solo) + 轨道名 |
| `LevelMeterCardView.swift` | 右侧独立电平卡片 — L/R竖向，绿→黄→红渐变，dB刻度 |

### 重构组件
| 文件 | 变更 |
|------|------|
| `MainWindowView.swift` | 完整布局重构为五区结构 |

### 布局架构
```
┌──────────────────────────────────────────────────────────────────┐
│  标题栏：[AudioRecord]  [录制目标]                     [导出 ▶]  │
├──────────────────────────────────────────────────────────────────┤
│  编辑工具栏：[裁剪] [标准化] [淡入] [淡出]                       │
├────────┬──────────────────────────────────────────┬──────────────┤
│ 轨道板  │  波形 + 时间刻度尺                        │  电平表卡片  │
│ [🔊][S]│  ~~~波形区域~~~                           │  L ▓▓░░     │
│        │  [缩放滚动条]                              │  R ▓▓░░     │
├────────┴──────────────────────────────────────────┴──────────────┤
│  控制面板：00:00.00  [▶] [● REC] [■]  48kHz·32bit·立体声         │
├──────────────────────────────────────────────────────────────────┤
│  状态栏                                                          │
└──────────────────────────────────────────────────────────────────┘
```

### 两态行为
| 状态 | 波形区 | 编辑工具栏 | 导出按钮 |
|------|--------|-----------|---------|
| 空闲 | 空波形 | 禁用灰色 | 禁用 |
| 录制中 | 实时滚动 | 禁用灰色 | 禁用 |
| 编辑态 | 静态可编辑 | **自动激活** | **激活** |

### 验收
- [x] 导出按钮位于标题栏右侧
- [x] 顶部有编辑工具栏，录制中禁用，录完自动激活
- [x] 采用轨道+轨道板模式，左侧有🔊/S控制按钮
- [x] 电平表为右侧竖向独立卡片
- [x] 底部控制区保持现有风格
- [x] 录制停止后零操作切换为编辑态
- [x] 选中已有音频文件时自动进入编辑态
- [x] 预留多轨道扩展能力
- [x] 编译通过
