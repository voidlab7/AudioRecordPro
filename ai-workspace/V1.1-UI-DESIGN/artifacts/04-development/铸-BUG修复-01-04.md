# 产出索引：铸·开发 — BUG-01~04 修复

- **作者**: 铸·开发
- **日期**: 2026-05-17
- **触发**: 鉴·QA 测试报告 BUG-01~04

## 编译状态

- **编译结果**: ✅ 成功（0 errors, 2 pre-existing warnings）

## 修复清单

### BUG-01 [CRITICAL] 编辑器入口链路接通

**改动 4 个文件，接通 5 环链路：**

```
RecordedFilesView (编辑按钮 UI + onEdit 回调)
  → RecordedFilesViewDelegate.didRequestEditFile (新增)
  → SidebarView 转发 (新增)
  → SidebarViewDelegate.didRequestEditFile (新增)
  → MainWindowView SidebarViewDelegate 实现转发 (新增)
  → MainWindowViewDelegate.didRequestEditFile (已有)
  → MainViewController.enterEditor (已有)
```

**文件行 UI 改造：**
- hover 时：formatBadge 隐藏，编辑按钮（pencil.line SF Symbol）出现
- 点击编辑按钮 → 触发 onEdit 回调
- 鼠标离开：编辑按钮隐藏，formatBadge 恢复

### BUG-02 [HIGH] hideEditor 约束丢失

**方案：从 remove/add 改为 isHidden 切换**

- showEditor: recordingContentView.isHidden = true（保留约束）
- hideEditor: recordingContentView.isHidden = false（约束未被销毁）
- 编辑器视图仍用 add/remove（生命周期由 EditorViewController 管理）

### BUG-03 [HIGH] 加载失败无反馈

- loadAudio catch 块增加: waveformView.loadError = 错误信息
- loadAudio catch 块增加: waveformView.isLoading = false + needsDisplay
- EditorWaveformView.loadError/isLoading 改为 internal set

### BUG-04 [MEDIUM] 锁定标志泄漏

- PCMBuffer 分配失败 → 清理 EditorViewController.currentlyEditingURL = nil
- 音频文件读取失败 → 清理 EditorViewController.currentlyEditingURL = nil

## 改动文件

| 文件 | 改动 |
|------|------|
| RecordedFilesView.swift | delegate 新增方法 + 文件行编辑按钮 + hover 切换 |
| SidebarView.swift | delegate 新增方法 + 转发实现 |
| MainWindowView.swift | SidebarViewDelegate 转发 + showEditor/hideEditor 改为 isHidden |
| EditorViewController.swift | loadAudio 错误处理 + 锁定标志清理 |
| EditorWaveformView.swift | isLoading/loadError 改为 internal set |
